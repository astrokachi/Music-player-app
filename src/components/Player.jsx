import React from 'react';

const Player = () => {
  return (
    <div
      className="fixed bottom-0 z-10 bg-opacity-40 w-full text-light px-4 bg-primary filter"
      style={{ backdropFilter: 'blur(4px)' }}
    >
      player
    </div>
  );
};

export default Player;
